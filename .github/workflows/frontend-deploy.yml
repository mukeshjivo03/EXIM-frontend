name: Exim-Frontend Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy-frontend:
    runs-on: self-hosted
    
    steps:
      - name: Run deployment script
        shell: powershell
        run: |
          C:\LiveProjects\JIVO-EXIM\EXIM-frontend
          .\deploy_frontend.bat